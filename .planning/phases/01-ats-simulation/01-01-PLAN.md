---
phase: 01-ats-simulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/ats-simulation/types/index.ts
  - src/features/ats-simulation/types/parser.types.ts
  - src/features/ats-simulation/types/scoring.types.ts
  - src/features/ats-simulation/types/platform.types.ts
autonomous: true

must_haves:
  truths:
    - "Type system supports all 8 ATS2 requirements"
    - "Parser can represent section extraction results"
    - "Scoring system supports weighted breakdown (Parsing 40%, Keywords 30%, Format 20%, Layout 10%)"
    - "Platform simulators have distinct configurations"
  artifacts:
    - path: "src/features/ats-simulation/types/index.ts"
      provides: "Type exports and shared interfaces"
      exports: ["ATSSection", "ATSField", "ATSScore", "PlatformConfig"]
    - path: "src/features/ats-simulation/types/parser.types.ts"
      provides: "Section parsing types"
      contains: ["SectionType", "ExtractedSection", "FieldExtraction"]
    - path: "src/features/ats-simulation/types/scoring.types.ts"
      provides: "Scoring and confidence types"
      contains: ["ScoreCategory", "ConfidenceScore", "RiskLevel"]
    - path: "src/features/ats-simulation/types/platform.types.ts"
      provides: "Platform simulation types"
      contains: ["PlatformType", "PlatformSimulator", "PlatformQuirks"]
  key_links:
    - from: "parser.types.ts"
      to: "scoring.types.ts"
      via: "FieldExtraction.confidence references ConfidenceScore"
    - from: "platform.types.ts"
      to: "scoring.types.ts"
      via: "PlatformSimulator returns ATSScore"
---

<objective>
Create comprehensive TypeScript type definitions for the ATS Simulation 2.0 engine.

Purpose: Strong typing foundation prevents runtime errors and enables IDE autocomplete for all ATS simulation features.
Output: Complete type system supporting section parsing, confidence scoring, layout detection, and platform simulation.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ats-simulation/01-RESEARCH.md

Key requirements to implement:
- ATS2-01: Section parsing (experience, education, skills extraction)
- ATS2-02: Field extraction confidence scoring
- ATS2-03: Header/footer detection warnings
- ATS2-04: Table detection penalty scoring
- ATS2-05: Column layout detection
- ATS2-07: ATS compatibility badge logic
- ATS2-08: Platform comparison (Workday vs Lever)
</context>

<tasks>

<task type="auto">
  <name>Create parser type definitions</name>
  <files>src/features/ats-simulation/types/parser.types.ts</files>
  <action>
Create comprehensive parser types supporting section extraction:

1. SectionType enum: EXPERIENCE, EDUCATION, SKILLS, PROJECTS, CERTIFICATIONS, SUMMARY, CUSTOM

2. ExtractedSection interface:
   - type: SectionType
   - title: string (original header text)
   - content: string
   - startIndex: number (character position)
   - endIndex: number
   - confidence: number (0-100)
   - fields: FieldExtraction[]

3. FieldExtraction interface:
   - fieldType: 'date_range' | 'company' | 'title' | 'location' | 'degree' | 'institution' | 'skill' | 'bullet'
   - value: string
   - rawText: string
   - confidence: ConfidenceScore
   - position: { start: number, end: number }

4. DateRange type with validation:
   - startDate: string
   - endDate: string | 'Present'
   - isValidFormat: boolean
   - formatDetected: 'MMM YYYY' | 'MM/YYYY' | 'YYYY' | 'unknown'

Include JSDoc comments explaining each field's purpose and typical values.
  </action>
  <verify>grep -q "interface ExtractedSection" src/features/ats-simulation/types/parser.types.ts && grep -q "enum SectionType" src/features/ats-simulation/types/parser.types.ts</verify>
  <done>Parser types exist with all required interfaces for section and field extraction</done>
</task>

<task type="auto">
  <name>Create scoring and confidence types</name>
  <files>src/features/ats-simulation/types/scoring.types.ts</files>
  <action>
Create scoring system types with weighted breakdown:

1. ScoreCategory enum with weights:
   - PARSING = 40
   - KEYWORDS = 30
   - FORMAT = 20
   - LAYOUT = 10

2. ATSScore interface:
   - overall: number (0-100)
   - breakdown: Record<ScoreCategory, CategoryScore>
   - timestamp: number
   - version: string

3. CategoryScore interface:
   - score: number (0-100)
   - weight: number
   - weightedContribution: number
   - issues: ScoreIssue[]

4. ScoreIssue interface:
   - severity: 'critical' | 'high' | 'medium' | 'low'
   - category: ScoreCategory
   - message: string
   - suggestion: string
   - affectedPlatforms: PlatformType[]

5. ConfidenceScore interface:
   - overall: number (0-100)
   - factors: ConfidenceFactor[]
   - reasoning: string

6. ConfidenceFactor type:
   - type: 'pattern_match' | 'context_proximity' | 'format_validity' | 'section_boundary'
   - weight: number
   - score: number
   - description: string

7. RiskLevel enum: CRITICAL, HIGH, MEDIUM, LOW, NONE

8. LayoutRisk interface:
   - type: 'table' | 'column' | 'header' | 'footer' | 'text_box' | 'graphic'
   - severity: RiskLevel
   - element: string (CSS selector or element type)
   - message: string
   - platformsAffected: PlatformType[]

Export all types with clear naming conventions.
  </action>
  <verify>grep -q "PARSING = 40" src/features/ats-simulation/types/scoring.types.ts && grep -q "interface ATSScore" src/features/ats-simulation/types/scoring.types.ts</verify>
  <done>Scoring types support 40/30/20/10 weighted breakdown with confidence factors</done>
</task>

<task type="auto">
  <name>Create platform simulation types</name>
  <files>src/features/ats-simulation/types/platform.types.ts</files>
  <action>
Create platform-specific simulation types:

1. PlatformType enum:
   - WORKDAY
   - TALEO
   - GREENHOUSE
   - LEVER
   - ICIMS
   - GENERIC

2. PlatformQuirks interface:
   - strictHeaders: boolean
   - allowedHeaders: string[]
   - tablePenalty: number (0-1, e.g., 0.4 for 40% penalty)
   - columnPenalty: number
   - dateFormatStrict: boolean
   - supportedDateFormats: string[]
   - headerDetection: 'strict' | 'flexible' | 'semantic'
   - footerSensitivity: RiskLevel

3. PlatformSimulator interface:
   - platform: PlatformType
   - quirks: PlatformQuirks
   - simulate: (resume: ResumeData) => ParsedResumeResult
   - calculateScore: (result: ParsedResumeResult) => PlatformScore

4. ParsedResumeResult interface:
   - platform: PlatformType
   - sections: ExtractedSection[]
   - extractionQuality: number (0-100)
   - dataLossWarnings: DataLossWarning[]
   - parsingErrors: ParsingError[]

5. DataLossWarning interface:
   - type: 'section_missed' | 'field_truncated' | 'date_unparsed' | 'content_duplicated'
   - severity: RiskLevel
   - originalText: string
   - extractedText: string | null
   - section: string

6. PlatformScore interface:
   - platform: PlatformType
   - score: number (0-100)
   - issues: ScoreIssue[]
   - riskLevel: RiskLevel

Include comprehensive JSDoc with real-world examples from research (e.g., Workday table penalty = 40%).
  </action>
  <verify>grep -q "PlatformType" src/features/ats-simulation/types/platform.types.ts && grep -q "tablePenalty" src/features/ats-simulation/types/platform.types.ts</verify>
  <done>Platform types support Workday, Taleo, Greenhouse, Lever with configurable quirks</done>
</task>

<task type="auto">
  <name>Create index barrel export</name>
  <files>src/features/ats-simulation/types/index.ts</files>
  <action>
Create central type export file:

1. Re-export all types from parser.types.ts
2. Re-export all types from scoring.types.ts
3. Re-export all types from platform.types.ts
4. Add shared utility types:
   - ATSRiskReport: Combined report type
   - ATSBadgeLevel: 'excellent' | 'good' | 'fair' | 'poor' (based on score ranges)

5. Add type guards:
   - isSectionType(value: unknown): value is SectionType
   - isPlatformType(value: unknown): value is PlatformType
   - isRiskLevel(value: unknown): value is RiskLevel

6. Add helper types:
   - ScoreRange: { min: number, max: number, label: ATSBadgeLevel }
   - ATSConfig: { scoring: ScoringConfig, platforms: PlatformConfig[] }

Include comprehensive module documentation comment at top explaining the type system architecture.
  </action>
  <verify>grep -q "export \* from './parser.types'" src/features/ats-simulation/types/index.ts && grep -q "ATSBadgeLevel" src/features/ats-simulation/types/index.ts</verify>
  <done>Central type barrel exports all ATS simulation types with helpers</done>
</task>

</tasks>

<verification>
- All type files compile without TypeScript errors
- Score weights sum to 100% (40+30+20+10)
- Platform quirks match research findings (Workday 40% table penalty)
- Type exports are discoverable via IDE autocomplete
</verification>

<success_criteria>
1. Type system supports all 8 ATS2 requirements
2. Score breakdown implements 40/30/20/10 weighting
3. Platform simulators have distinct configurations for Workday, Taleo, Greenhouse, Lever
4. All types exported through central index.ts
5. No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-ats-simulation/01-01-SUMMARY.md`
</output>
