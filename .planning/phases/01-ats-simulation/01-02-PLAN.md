---
phase: 01-ats-simulation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/features/ats-simulation/engine/sectionExtractor.ts
  - src/features/ats-simulation/engine/__tests__/sectionExtractor.test.ts
autonomous: true

must_haves:
  truths:
    - "Section extractor identifies Experience, Education, Skills sections"
    - "Date ranges extracted with 90%+ accuracy"
    - "Company names identified in context"
    - "Field extraction provides confidence scores"
  artifacts:
    - path: "src/features/ats-simulation/engine/sectionExtractor.ts"
      provides: "Rule-based section extraction logic"
      exports: ["extractSections", "extractFields", "calculateConfidence"]
    - path: "src/features/ats-simulation/engine/__tests__/sectionExtractor.test.ts"
      provides: "Unit tests for extraction accuracy"
      min_tests: 10
  key_links:
    - from: "test file"
      to: "sectionExtractor.ts"
      via: "import { extractSections }"
---

<objective>
Implement rule-based section extraction engine with confidence scoring.

Purpose: Core parsing logic that simulates how ATS systems extract resume sections and fields (ATS2-01, ATS2-02).
Output: Working section extractor with >85% accuracy via TDD approach.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-ats-simulation/01-RESEARCH.md
@src/features/ats-simulation/types/index.ts (from 01-01)

Research insights:
- Section headers: "Work Experience", "Professional Experience", "Employment History", "Career"
- Date patterns: MMM YYYY - MMM YYYY, MM/YY - Present, YYYY - YYYY
- Education indicators: "University", "College", "Bachelor", "Master", "BS", "MS"
- Skills formats: Bullet lists or comma-separated technical terms
- Confidence weights: pattern_match (30%), context_proximity (25%), format_validity (25%), section_boundary (20%)
</context>

<feature>
  <name>Section Extraction Engine</name>
  <files>
    src/features/ats-simulation/engine/sectionExtractor.ts
    src/features/ats-simulation/engine/__tests__/sectionExtractor.test.ts
  </files>
  
  <behavior>
    extractSections(resumeText: string): ExtractedSection[]
    
    Test cases:
    1. Input: "WORK EXPERIENCE\nSoftware Engineer at Google\nJan 2020 - Present"
       Expected: 1 section, type=EXPERIENCE, fields include date_range and company
       
    2. Input: "EDUCATION\nBS Computer Science\nUniversity of Washington\n2015-2019"
       Expected: 1 section, type=EDUCATION, fields include degree and institution
       
    3. Input: "SKILLS\nJavaScript, React, Node.js"
       Expected: 1 section, type=SKILLS, fields include skill items
       
    4. Input: Mixed sections with non-standard headers
       "Professional Journey\n...\nAcademic Background\n..."
       Expected: Maps "Professional Journey" -> EXPERIENCE, "Academic Background" -> EDUCATION
       
    5. Missing section detection
       Input: Resume with no clear Experience header
       Expected: Returns sections with confidence < 50, flags uncertain extraction
  </behavior>
  
  <implementation>
1. RED Phase: Write comprehensive tests first
   - Test all section type detection (Experience, Education, Skills, etc.)
   - Test date extraction with multiple formats
   - Test confidence scoring calculation
   - Test edge cases (missing headers, ambiguous content)

2. GREEN Phase: Minimal implementation to pass tests
   - Create regex patterns for section headers (case-insensitive)
   - Implement date pattern matching (MMM YYYY, MM/YY, YYYY)
   - Build confidence calculator with weighted factors
   - Handle section boundary detection

3. REFACTOR Phase: Clean up while keeping tests green
   - Extract pattern constants
   - Optimize regex compilation
   - Add input validation
   - Improve error handling

Pattern library to implement:
- SECTION_PATTERNS: Record<SectionType, RegExp[]>
- DATE_PATTERNS: RegExp[] for various date formats
- FIELD_PATTERNS: Record<FieldType, RegExp>
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>RED: Write failing tests for section extraction</name>
  <files>src/features/ats-simulation/engine/__tests__/sectionExtractor.test.ts</files>
  <action>
Create comprehensive test suite following TDD principles:

1. Section Detection Tests:
   - Standard headers: "Work Experience", "Education", "Skills"
   - Variations: "Professional Experience", "Academic Background", "Technical Skills"
   - Non-standard: "Career History", "Qualifications", "Expertise"
   - Case insensitivity: "work experience", "EDUCATION"

2. Field Extraction Tests:
   - Date ranges: "Jan 2020 - Dec 2022", "01/20 - Present", "2020-2022"
   - Company detection: Context-aware (near dates/titles)
   - Job titles: Title case patterns near companies
   - Degrees: "BS", "Bachelor of Science", "MBA"
   - Institutions: "University of...", "College"

3. Confidence Scoring Tests:
   - High confidence: Standard format, clear headers, valid dates
   - Medium confidence: Non-standard header but clear content
   - Low confidence: Ambiguous section, missing dates
   - Verify weighted calculation: 30% pattern + 25% context + 25% format + 20% boundary

4. Edge Case Tests:
   - Empty resume
   - No recognizable sections
   - Duplicate section headers
   - Very long content between sections
   - Special characters and formatting

Use describe/it blocks with clear test names. Tests should FAIL initially (RED phase).
  </action>
  <verify>npm test -- sectionExtractor.test.ts 2>&1 | grep -q "failing\|FAIL"</verify>
  <done>Test file exists with 10+ failing tests covering all extraction scenarios</done>
</task>

<task type="auto">
  <name>GREEN: Implement section extractor to pass tests</name>
  <files>src/features/ats-simulation/engine/sectionExtractor.ts</files>
  <action>
Implement section extraction engine to make all tests pass:

1. Pattern Definitions:
```typescript
const SECTION_PATTERNS: Record<SectionType, RegExp[]> = {
  EXPERIENCE: [
    /work\s+experience/i,
    /professional\s+experience/i,
    /employment\s+history/i,
    /career(\s+history)?/i,
    /professional\s+journey/i
  ],
  EDUCATION: [
    /education/i,
    /academic(\s+background)?/i,
    /qualifications/i,
    /degrees?/i
  ],
  SKILLS: [
    /skills?/i,
    /technical\s+skills/i,
    /core\s+competencies/i,
    /expertise/i
  ],
  // ... other sections
};

const DATE_PATTERNS = [
  /\b(Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)[a-z]*\.?\s+\d{4}\b/gi,
  /\b\d{1,2}\/\d{2,4}\b/g,
  /\b\d{4}\s*-\s*(\d{4}|Present)\b/gi
];
```

2. Core Functions:
   - detectSections(text: string): SectionBoundary[]
   - extractSectionContent(boundary: SectionBoundary): string
   - extractFields(content: string, type: SectionType): FieldExtraction[]
   - calculateConfidence(field: FieldExtraction): ConfidenceScore

3. Confidence Calculation:
   - Pattern match: Did regex find it? (30 points)
   - Context proximity: Is it near expected content? (25 points)
   - Format validity: Does it match expected format? (25 points)
   - Section boundary: Is it clearly within section? (20 points)

4. Date Parsing:
   - normalizeDate(dateStr: string): Date | null
   - parseDateRange(rangeStr: string): DateRange | null
   - validateDateFormat(date: DateRange): boolean

Keep implementation minimal but functional. All tests must PASS.
  </action>
  <verify>npm test -- sectionExtractor.test.ts 2>&1 | grep -q "passing\|PASS"</verify>
  <done>All tests pass with minimal extraction implementation</done>
</task>

<task type="auto">
  <name>REFACTOR: Clean up and optimize</name>
  <files>src/features/ats-simulation/engine/sectionExtractor.ts</files>
  <action>
Refactor while keeping tests green:

1. Extract Constants:
   - Move all regex patterns to separate constants file
   - Create configurable pattern weights
   - Add section type priority ordering

2. Optimize Performance:
   - Compile regexes once, reuse
   - Cache section detection results
   - Use early returns for obvious non-matches

3. Add Error Handling:
   - Validate inputs (null, empty string checks)
   - Handle regex exceptions gracefully
   - Return empty array with warning for invalid input

4. Improve Readability:
   - Extract helper functions: isDateRange(), isCompanyName(), etc.
   - Add inline comments for complex logic
   - Use descriptive variable names

5. Add Logging (debug mode):
   - Log pattern matches for troubleshooting
   - Track confidence calculation steps
   - Performance timing for extraction

Verify: npm test still passes after refactoring.
  </action>
  <verify>npm test -- sectionExtractor.test.ts 2>&1 | grep -E "(passing|PASS|✓)" | tail -1</verify>
  <done>Code refactored with tests still passing, improved organization and performance</done>
</task>

</tasks>

<verification>
- All unit tests pass (RED→GREEN→REFACTOR complete)
- Section detection accuracy >85% on test cases
- Date extraction handles MMM YYYY, MM/YY, YYYY formats
- Confidence scores properly weighted (30/25/25/20)
- Code follows existing project conventions
</verification>

<success_criteria>
1. Test suite has 10+ tests covering major scenarios
2. All tests pass consistently
3. Section extractor handles Experience, Education, Skills sections
4. Date extraction accuracy >90%
5. Confidence scoring implemented with weighted factors
6. Code is clean, commented, and maintainable
</success_criteria>

<output>
After completion, create `.planning/phases/01-ats-simulation/01-02-SUMMARY.md`
</output>
