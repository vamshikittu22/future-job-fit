---
phase: 02-match-intelligence
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Recommendations are specific: 'Add Kubernetes to skills section'"
    - "Recommendations show expected score improvement"
    - "Comparison view highlights matched vs missing keywords"
  artifacts:
    - path: "src/features/match-intelligence/components/RecommendationsPanel.tsx"
      provides: "Actionable recommendations list"
    - path: "src/features/match-intelligence/components/JDComparisonView.tsx"
      provides: "Side-by-side keyword comparison"
  key_links:
    - from: "src/features/match-intelligence/components/RecommendationsPanel.tsx"
      to: "src/features/match-intelligence/hooks/useSkillClusters.ts"
      via: "gap analysis data"
      pattern: "useSkillClusters"
---

<objective>
Build impact recommendations panel and JD comparison view (MATCH-06, MATCH-08).

Purpose: Provide specific actionable recommendations and side-by-side comparison.
Output: Two React components with specific recommendations and keyword diff.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-match-intelligence/02-RESEARCH.md
@.planning/phases/02-match-intelligence/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build RecommendationsPanel component</name>
  <files>src/features/match-intelligence/components/RecommendationsPanel.tsx</files>
  <action>
    Create `src/features/match-intelligence/components/RecommendationsPanel.tsx`:

    ```typescript
    import React from 'react';
    import { Card, CardHeader, CardTitle, CardContent } from '@/shared/ui/Card';
    import { Badge } from '@/shared/ui/Badge';
    import { Button } from '@/shared/ui/Button';
    import type { ImpactRecommendation } from '../types';

    interface RecommendationsPanelProps {
      recommendations: ImpactRecommendation[];
      maxItems?: number;
    }

    const impactConfig = {
      high: { label: 'High Impact', color: 'text-green-600', bg: 'bg-green-100 dark:bg-green-900' },
      medium: { label: 'Medium Impact', color: 'text-yellow-600', bg: 'bg-yellow-100 dark:bg-yellow-900' },
      low: { label: 'Low Impact', color: 'text-gray-600', bg: 'bg-gray-100 dark:bg-gray-900' },
    };

    const typeIcons: Record<string, string> = {
      add_skill: '➕',
      enhance_experience: '✨',
      add_metrics: '📊',
      reorder: '🔄',
      quantify: '📈',
    };

    export function RecommendationsPanel({ 
      recommendations, 
      maxItems = 10 
    }: RecommendationsPanelProps) {
      const displayRecs = recommendations.slice(0, maxItems);

      if (displayRecs.length === 0) {
        return (
          <Card>
            <CardContent className="py-8 text-center text-muted-foreground">
              No recommendations available. Your resume looks great!
            </CardContent>
          </Card>
        );
      }

      return (
        <Card>
          <CardHeader>
            <CardTitle>Recommendations</CardTitle>
            <p className="text-sm text-muted-foreground">
              Actions to improve your match score
            </p>
          </CardHeader>
          <CardContent className="space-y-3">
            {displayRecs.map((rec) => {
              const config = impactConfig[rec.impact];
              return (
                <div
                  key={rec.id}
                  className="p-4 border rounded-lg hover:bg-muted/30 transition-colors"
                >
                  <div className="flex items-start gap-3">
                    <span className="text-xl">{typeIcons[rec.type] || '💡'}</span>
                    <div className="flex-1 min-w-0">
                      <div className="flex items-center gap-2 mb-1">
                        <span className="font-medium text-sm">{rec.title}</span>
                        <Badge 
                          variant="outline" 
                          className={`text-xs ${config.color}`}
                        >
                          +{rec.expectedScoreImprovement} pts
                        </Badge>
                      </div>
                      <p className="text-sm text-muted-foreground mb-2">
                        {rec.description}
                      </p>
                      <div className="flex items-center gap-2">
                        <Badge className={config.bg}>
                          {config.label}
                        </Badge>
                        {rec.targetLocation && (
                          <span className="text-xs text-muted-foreground">
                            {rec.targetLocation}
                          </span>
                        )}
                      </div>
                    </div>
                  </div>
                </div>
              );
            })}

            {recommendations.length > maxItems && (
              <div className="text-center pt-2">
                <Button variant="outline" size="sm">
                  View All {recommendations.length} Recommendations
                </Button>
              </div>
            )}

            {/* Summary */}
            <div className="pt-4 border-t">
              <div className="flex items-center justify-between text-sm">
                <span className="text-muted-foreground">
                  Total potential improvement
                </span>
                <span className="font-bold text-green-600">
                  +{recommendations.reduce((sum, r) => sum + r.expectedScoreImprovement, 0)} pts
                </span>
              </div>
            </div>
          </CardContent>
        </Card>
      );
    }

    export default RecommendationsPanel;
    ```

    Add ImpactRecommendation type:
    ```typescript
    // Add to types/index.ts
    export interface ImpactRecommendation {
      id: string;
      type: 'add_skill' | 'enhance_experience' | 'add_metrics' | 'reorder' | 'quantify';
      impact: 'high' | 'medium' | 'low';
      title: string;
      description: string;
      specificAction: string;
      targetLocation?: string;
      expectedScoreImprovement: number;
    }
    ```
  </action>
  <verify>
    Check TypeScript compilation
  </verify>
  <done>
    RecommendationsPanel shows actionable recommendations with impact scores
  </done>
</task>

<task type="auto">
  <name>Task 2: Build JDComparisonView component</name>
  <files>src/features/match-intelligence/components/JDComparisonView.tsx</files>
  <action>
    Create `src/features/match-intelligence/components/JDComparisonView.tsx`:

    ```typescript
    import React, { useState } from 'react';
    import { Card, CardHeader, CardTitle, CardContent } from '@/shared/ui/Card';
    import { Button } from '@/shared/ui/Button';
    import type { ComparisonItem, JDComparisonData } from '../types';

    interface JDComparisonViewProps {
      comparison: JDComparisonData;
    }

    const statusConfig = {
      matched: { 
        icon: '✓', 
        className: 'text-green-600 bg-green-50 dark:bg-green-950' 
      },
      partial: { 
        icon: '~', 
        className: 'text-yellow-600 bg-yellow-50 dark:bg-yellow-950' 
      },
      missing: { 
        icon: '✗', 
        className: 'text-red-600 bg-red-50 dark:bg-red-950' 
      },
    };

    export function JDComparisonView({ comparison }: JDComparisonViewProps) {
      const [viewMode, setViewMode] = useState<'unified' | 'split'>('unified');

      const { items, matchedCount, partialCount, missingCount } = comparison;

      if (items.length === 0) {
        return (
          <Card>
            <CardContent className="py-8 text-center text-muted-foreground">
              No job description loaded. Add a job to see comparison.
            </CardContent>
          </Card>
        );
      }

      // Group items by status
      const missing = items.filter(i => i.status === 'missing');
      const partial = items.filter(i => i.status === 'partial');
      const matched = items.filter(i => i.status === 'matched');

      return (
        <Card>
          <CardHeader>
            <div className="flex items-center justify-between">
              <CardTitle>JD Comparison</CardTitle>
              <div className="flex gap-2">
                <Button 
                  variant={viewMode === 'unified' ? 'default' : 'ghost'}
                  size="sm"
                  onClick={() => setViewMode('unified')}
                >
                  Unified
                </Button>
                <Button 
                  variant={viewMode === 'split' ? 'default' : 'ghost'}
                  size="sm"
                  onClick={() => setViewMode('split')}
                >
                  Split
                </Button>
              </div>
            </div>
          </CardHeader>
          <CardContent className="space-y-4">
            {/* Summary stats */}
            <div className="flex gap-4 text-sm">
              <div className="flex items-center gap-1">
                <span className="w-3 h-3 rounded-full bg-green-500"></span>
                <span>Matched ({matchedCount})</span>
              </div>
              <div className="flex items-center gap-1">
                <span className="w-3 h-3 rounded-full bg-yellow-500"></span>
                <span>Partial ({partialCount})</span>
              </div>
              <div className="flex items-center gap-1">
                <span className="w-3 h-3 rounded-full bg-red-500"></span>
                <span>Missing ({missingCount})</span>
              </div>
            </div>

            {viewMode === 'unified' ? (
              // Unified view
              <div className="space-y-2">
                {missing.length > 0 && (
                  <div className="space-y-1">
                    <h4 className="text-sm font-medium text-red-600">Missing</h4>
                    {missing.map((item, idx) => (
                      <div 
                        key={`missing-${idx}`}
                        className="flex items-center gap-2 p-2 rounded bg-red-50 dark:bg-red-950"
                      >
                        <span className="text-red-600">✗</span>
                        <span className="flex-1">{item.keyword}</span>
                        <span className="text-xs text-muted-foreground capitalize">{item.type}</span>
                      </div>
                    ))}
                  </div>
                )}

                {partial.length > 0 && (
                  <div className="space-y-1">
                    <h4 className="text-sm font-medium text-yellow-600">Partial</h4>
                    {partial.map((item, idx) => (
                      <div 
                        key={`partial-${idx}`}
                        className="flex items-center gap-2 p-2 rounded bg-yellow-50 dark:bg-yellow-950"
                      >
                        <span className="text-yellow-600">~</span>
                        <span className="flex-1">{item.keyword}</span>
                        <span className="text-xs text-muted-foreground">{item.resumeText}</span>
                      </div>
                    ))}
                  </div>
                )}

                {matched.length > 0 && (
                  <div className="space-y-1">
                    <h4 className="text-sm font-medium text-green-600">Matched</h4>
                    {matched.map((item, idx) => (
                      <div 
                        key={`matched-${idx}`}
                        className="flex items-center gap-2 p-2 rounded bg-green-50 dark:bg-green-950"
                      >
                        <span className="text-green-600">✓</span>
                        <span className="flex-1">{item.keyword}</span>
                      </div>
                    ))}
                  </div>
                )}
              </div>
            ) : (
              // Split view
              <div className="grid grid-cols-2 gap-4">
                <div className="space-y-1">
                  <h4 className="text-sm font-medium mb-2">Resume</h4>
                  {items.filter(i => i.status !== 'missing').map((item, idx) => (
                    <div key={`resume-${idx}`} className="flex items-center gap-2 p-2 rounded bg-muted">
                      <span className="text-green-600">✓</span>
                      <span className="text-sm">{item.keyword}</span>
                    </div>
                  ))}
                </div>
                <div className="space-y-1">
                  <h4 className="text-sm font-medium mb-2">Job Description</h4>
                  {items.map((item, idx) => {
                    const config = statusConfig[item.status];
                    return (
                      <div 
                        key={`jd-${idx}`}
                        className={`flex items-center gap-2 p-2 rounded ${config.className}`}
                      >
                        <span>{config.icon}</span>
                        <span className="text-sm flex-1">{item.keyword}</span>
                        <span className="text-xs text-muted-foreground capitalize">{item.type}</span>
                      </div>
                    );
                  })}
                </div>
              </div>
            )}

            {/* Progress bar */}
            <div className="pt-4 border-t">
              <div className="flex justify-between text-sm mb-2">
                <span>Match Rate</span>
                <span className="font-medium">{Math.round((matchedCount / items.length) * 100)}%</span>
              </div>
              <div className="h-2 bg-muted rounded-full overflow-hidden flex">
                <div className="h-full bg-green-500" style={{ width: `${(matchedCount / items.length) * 100}%` }} />
                <div className="h-full bg-yellow-500" style={{ width: `${(partialCount / items.length) * 100}%` }} />
                <div className="h-full bg-red-500" style={{ width: `${(missingCount / items.length) * 100}%` }} />
              </div>
            </div>
          </CardContent>
        </Card>
      );
    }

    export default JDComparisonView;
    ```

    Add comparison types to types/index.ts:
    ```typescript
    export interface ComparisonItem {
      keyword: string;
      resumeText?: string;
      jdRequirement: string;
      status: 'matched' | 'partial' | 'missing';
      type: 'required' | 'preferred';
      category: string;
    }

    export interface JDComparisonData {
      items: ComparisonItem[];
      matchedCount: number;
      partialCount: number;
      missingCount: number;
    }
    ```
  </action>
  <verify>
    Check TypeScript compilation
  </verify>
  <done>
    JDComparisonView shows keyword matches with unified and split views
  </done>
</task>

<task type="auto">
  <name>Task 3: Create main module barrel exports</name>
  <files>src/features/match-intelligence/index.ts</files>
  <action>
    Create `src/features/match-intelligence/index.ts`:

    ```typescript
    // Main module exports
    export * from './types';
    export * from './hooks';
    export * from './components';
    export * from './utils';

    // Re-export for convenience
    export { useMatchScore } from './hooks/useMatchScore';
    export { useSkillClusters } from './hooks/useSkillClusters';
    export { useSemanticSimilarity } from './hooks/useSemanticSimilarity';
    export { useRecruiterHeatmap } from './hooks/useRecruiterHeatmap';

    // Components
    export { SkillClusteringViz } from './components/SkillClusteringViz';
    export { CompetencyGapList } from './components/CompetencyGapList';
    export { SemanticScoreCard } from './components/SemanticScoreCard';
    export { RecruiterHeatmap } from './components/RecruiterHeatmap';
    export { RecommendationsPanel } from './components/RecommendationsPanel';
    export { JDComparisonView } from './components/JDComparisonView';
    ```
  </action>
  <verify>
    Check module exports resolve correctly
  </verify>
  <done>
    Module barrel exports complete
  </done>
</task>

</tasks>

<verification>
- Recommendations show specific actions with score improvement
- Comparison view has unified and split modes
- Progress bar shows match rate
- Missing keywords displayed prominently
</verification>

<success_criteria>
1. Recommendations are specific: "Add X to Y section"
2. Each recommendation shows expected score improvement
3. Comparison view toggles between unified and split modes
4. Match rate percentage displayed
5. Color-coded by match status
</success_criteria>

<output>
After completion, create `.planning/phases/02-match-intelligence/02-05-SUMMARY.md`
</output>
