---
phase: 03-job-optimizer-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/job-optimizer/components/ResumePanelV2.tsx
  - src/features/job-optimizer/hooks/useResumeUpload.ts
autonomous: true

must_haves:
  truths:
    - "User can upload resume file or paste text in left panel"
    - "Resume summary displays after upload (name, email, phone, summary excerpt)"
    - "User can clear uploaded resume and start over"
  artifacts:
    - path: "src/features/job-optimizer/components/ResumePanelV2.tsx"
      provides: "Left panel with resume upload, paste, and summary display"
      min_lines: 120
    - path: "src/features/job-optimizer/hooks/useResumeUpload.ts"
      provides: "Resume upload and text extraction logic"
      exports: ["useResumeUpload"]
  key_links:
    - from: "ResumePanelV2"
      to: "JobContext"
      via: "useJob hook for resume text state"
      pattern: "useJob\\(\\)"
    - from: "ResumePanelV2"
      to: "ResumeContext"
      via: "useResume for resume data"
      pattern: "useResume\\(\\)"
    - from: "useResumeUpload"
      to: "FileReader API"
      via: "readAsText for file content extraction"
      pattern: "FileReader.*readAsText"
---

<objective>
Build the left Resume Panel with upload, paste, and summary display functionality—the first of three panels in the new layout.

Purpose: Enable users to provide resume data via multiple input methods and see a quick summary without leaving the Job Optimizer page.
Output: Fully functional resume input panel with file upload, paste area, and summary card display.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-job-optimizer-ui/03-RESEARCH.md
@src/shared/contexts/JobContext.tsx
@src/shared/contexts/ResumeContext.tsx
@src/shared/ui/card.tsx
@src/shared/ui/button.tsx
@src/shared/ui/textarea.tsx
</context>

<tasks>

<task type="auto">
  <name>Create useResumeUpload hook</name>
  <files>src/features/job-optimizer/hooks/useResumeUpload.ts</files>
  <action>
Create custom hook for resume file upload and text extraction.

Hook requirements:
- Return type: { uploadFile: (file: File) => Promise<string>, extractResumeSummary: (text: string) => { name?: string, email?: string, phone?: string, summaryExcerpt?: string } }
- uploadFile: Accepts File object, uses FileReader to read as text, returns Promise<string>
- extractResumeSummary: Simple regex extraction for name (first line), email (email pattern), phone (phone pattern), summary (first 150 chars)
- Handle file types: .txt, .pdf (text-only—no complex PDF parsing), .docx (not supported—show toast)
- Error handling: Reject promise if file read fails, show toast for unsupported types

Implementation:
- Use FileReader API for file upload
- Basic regex patterns: email = /\S+@\S+\.\S+/, phone = /\+?\d{1,3}?[-.\s]?\(?\d{1,4}?\)?[-.\s]?\d{1,4}[-.\s]?\d{1,9}/
- extractResumeSummary returns object with optional fields (may not find all data)
- Export named: export function useResumeUpload()
  </action>
  <verify>
Check hook implementation:
- TypeScript compiles with no errors
- Hook exported as named export
- uploadFile handles .txt files correctly
- extractResumeSummary returns object with expected shape
- Error cases handled with proper error messages
  </verify>
  <done>useResumeUpload.ts exists, compiles, extracts text from files, parses basic contact info from resume text</done>
</task>

<task type="auto">
  <name>Create ResumePanelV2 component</name>
  <files>src/features/job-optimizer/components/ResumePanelV2.tsx</files>
  <action>
Create left panel component for resume upload and summary display using research architecture from 03-RESEARCH.md.

Component structure:
- Import: PanelHeader, EmptyStatePrompt (from 03-01), useResumeUpload, useJob, useResume, Card, Button, Textarea
- State: resumeText (string), resumeSummary (object from extractResumeSummary)
- Sync with JobContext: Read/write to currentJob.resumeText via useJob() hook
- Layout: Card with h-full flex flex-col

Three display states:
1. Empty state (no resume): EmptyStatePrompt with FileUp icon, title "Upload Your Resume", description "Drag & drop a file or paste your resume text to begin analysis", action "Upload File" (triggers file input)
2. Input state (no summary yet): PanelHeader + file upload button + Textarea for paste (placeholder: "Or paste your resume text here...") + "Analyze Resume" button
3. Summary state (has resume): PanelHeader with badge "Uploaded" + Card showing name, email, phone, summary excerpt + "Edit" button (returns to input state) + "Clear" button

File upload:
- Hidden input type="file" accept=".txt,.pdf"
- Trigger via ref on "Upload File" button click
- On file select: uploadFile(file) -> set resumeText -> extractResumeSummary -> save to JobContext

Paste input:
- Textarea with value bound to resumeText
- onChange updates resumeText state
- "Analyze Resume" button calls extractResumeSummary and saves to JobContext

Clear action:
- Resets resumeText state to ""
- Clears JobContext.currentJob.resumeText
- Shows toast "Resume cleared"

Export as default.
  </action>
  <verify>
Check component implementation:
- TypeScript compiles with no errors
- Component exported as default
- Renders three distinct states based on data presence
- File upload triggers FileReader via useResumeUpload hook
- Textarea paste updates local state and JobContext
- Clear button resets state and context
  </verify>
  <done>ResumePanelV2.tsx exists, compiles, displays empty/input/summary states, uploads files, parses text, syncs with JobContext, clears data on user action</done>
</task>

</tasks>

<verification>
1. Both files exist in correct locations
2. TypeScript compilation passes (npm run build)
3. ResumePanelV2 renders in three states: empty, input, summary
4. File upload extracts text from .txt files
5. Paste textarea updates JobContext
6. Resume summary displays name, email, phone, excerpt when parsed
7. Clear button resets state and JobContext
</verification>

<success_criteria>
1. User can upload .txt file and see extracted text
2. User can paste resume text in textarea
3. Resume summary card displays parsed contact info and excerpt
4. Edit button returns to input state from summary state
5. Clear button resets all resume data and shows toast confirmation
6. All state syncs with JobContext for use by other panels
</success_criteria>

<output>
After completion, create `.planning/phases/03-job-optimizer-ui/03-02-SUMMARY.md`
</output>
