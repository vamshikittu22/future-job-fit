---
phase: 03-job-optimizer-ui
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src/features/job-optimizer/components/MatchComparisonPanel.tsx
  - src/features/job-optimizer/hooks/useMatchComparison.ts
autonomous: true

must_haves:
  truths:
    - "User sees ATS score when both resume and JD are provided"
    - "Match panel shows keyword gaps between resume and JD"
    - "Match panel displays semantic similarity score"
    - "Optimization actions appear with specific suggestions"
  artifacts:
    - path: "src/features/job-optimizer/components/MatchComparisonPanel.tsx"
      provides: "Bottom-right panel with resume vs JD comparison"
      min_lines: 180
    - path: "src/features/job-optimizer/hooks/useMatchComparison.ts"
      provides: "Resume vs JD matching logic"
      exports: ["useMatchComparison"]
  key_links:
    - from: "MatchComparisonPanel"
      to: "JobContext + ResumeContext"
      via: "useJob and useResume hooks"
      pattern: "use(Job|Resume)\\(\\)"
    - from: "useMatchComparison"
      to: "Phase 2 match intelligence utilities"
      via: "weighted scoring from 02-match-intelligence"
      pattern: "calculateWeightedScore|semanticSimilarity"
---

<objective>
Build the bottom-right Match Comparison Panel showing ATS score, keyword gaps, semantic similarity, and optimization actions—completing the 3-panel architecture.

Purpose: Consolidate resume vs JD comparison features into a dedicated panel, separate from JD-only analysis, using Phase 2 match intelligence infrastructure.
Output: Fully functional match comparison panel with ATS scoring, gap analysis, and actionable recommendations.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-job-optimizer-ui/03-RESEARCH.md
@.planning/phases/02-match-intelligence/02-01-SUMMARY.md
@.planning/phases/02-match-intelligence/02-03-SUMMARY.md
@src/shared/contexts/JobContext.tsx
@src/shared/contexts/ResumeContext.tsx
@src/shared/ui/tabs.tsx
@src/shared/ui/card.tsx
@src/shared/ui/progress.tsx
</context>

<tasks>

<task type="auto">
  <name>Create useMatchComparison hook</name>
  <files>src/features/job-optimizer/hooks/useMatchComparison.ts</files>
  <action>
Create custom hook for resume vs JD matching analysis, leveraging Phase 2 match intelligence infrastructure.

Hook requirements:
- Input: resumeText (string), jobDescription (string)
- Return type: { atsScore: number, matchBreakdown: MatchBreakdown, keywordGaps: KeywordGap[], semanticSimilarity: number, recommendations: Recommendation[], isAnalyzing: boolean }
- Use Phase 2 utilities if available, otherwise implement simplified versions

Analysis functions:
1. calculateATSScore(resume: string, jd: string): Weighted scoring—50% keyword match, 25% semantic similarity, 15% format quality, 10% length appropriateness (from Phase 2 02-01)
2. findKeywordGaps(resume: string, jd: string): Compare JD keywords (from JD analyzer) against resume text, return missing keywords with importance weights (required > preferred)
3. calculateSemanticSimilarity(resume: string, jd: string): Simple TF-IDF cosine similarity or fallback to keyword overlap percentage (from Phase 2 02-03)
4. generateRecommendations(gaps: KeywordGap[], atsScore: number): Map gaps to actionable suggestions—"Add 'Kubernetes' to Skills section", "Highlight 'team leadership' in Experience bullets"

Implementation:
- Use useMemo with 500ms debounce for expensive calculations
- Check if Phase 2 utilities exist: src/shared/lib/match/weightedScoring.ts, src/shared/lib/match/semanticSimilarity.ts
- If Phase 2 utilities exist, import and use them; otherwise, implement simplified versions inline
- Return isAnalyzing: true during debounce period
- Export named: export function useMatchComparison(resumeText: string, jobDescription: string)

Types:
- MatchBreakdown = { keywords: number, semantic: number, format: number, length: number }
- KeywordGap = { keyword: string, importance: 'required' | 'preferred', suggestions: string[] }
- Recommendation = { type: 'add_keyword' | 'improve_section' | 'adjust_format', priority: 'high' | 'medium' | 'low', action: string }
  </action>
  <verify>
Check hook implementation:
- TypeScript compiles with no errors
- Hook exported as named export
- Returns atsScore (0-100), matchBreakdown, keywordGaps array, semanticSimilarity, recommendations
- Debounces calculations with 500ms delay
- Attempts to use Phase 2 utilities if available, falls back to simplified logic
- Recommendations are specific and actionable
  </verify>
  <done>useMatchComparison.ts exists, compiles, calculates ATS score, identifies keyword gaps, computes semantic similarity, generates recommendations, debounces expensive operations</done>
</task>

<task type="auto">
  <name>Create MatchComparisonPanel component</name>
  <files>src/features/job-optimizer/components/MatchComparisonPanel.tsx</files>
  <action>
Create bottom-right panel component for resume vs JD comparison with tabbed sections.

Component structure:
- Import: PanelHeader, EmptyStatePrompt (from 03-01), useMatchComparison, useJob, useResume, Tabs, Card, Progress, Badge, ScrollArea
- Read data: const { currentJob } = useJob(), const { resumeData } = useResume()
- Derive text: resumeText from resumeData (serialize to string), jobDescription from currentJob?.description
- Pass both to useMatchComparison hook
- Layout: Card with h-full flex flex-col

Three display states:
1. Empty state (no resume or JD): EmptyStatePrompt with Target icon, title "Match Analysis Ready", description "Upload resume and paste JD to see how well they align", no action (prompts handled in other panels)
2. Partial state (only resume OR only JD): EmptyStatePrompt with AlertCircle icon, title "Need Both Resume & JD", description showing what's missing
3. Analysis state (has both): PanelHeader with Target icon, title "Match Analysis", badge showing ATS score (colored: green >80, yellow 60-80, red <60) + Tabs component

Tabs structure:
- TabsList with 4 triggers: Score, Gaps, Similarity, Actions (grid grid-cols-4)
- ScrollArea wrapping TabsContent sections

Tab 1 - Score:
- Large ATS score display (text-6xl font-bold with color based on score)
- Progress bar for overall match (variant based on score)
- Breakdown bars: Keywords (50%), Semantic (25%), Format (15%), Length (10%)
- Each breakdown: label + Progress component + percentage text

Tab 2 - Gaps:
- List of missing keywords grouped by importance
- Required gaps: Badge variant="destructive" with keyword
- Preferred gaps: Badge variant="secondary" with keyword
- Each gap shows quick suggestions inline (small text, muted)

Tab 3 - Similarity:
- Circular progress for semantic similarity score (0-100)
- Text explanation: "Your resume content aligns X% with the job requirements"
- Top matching phrases list (if available from hook)

Tab 4 - Actions:
- List of recommendations sorted by priority
- Each recommendation: priority badge (High/Medium/Low) + action text
- Use Alert component for high-priority actions (variant="destructive" for high, "default" for others)
- CTA button per action if applicable (e.g., "Add to Resume" opens wizard)

Export as default.
  </action>
  <verify>
Check component implementation:
- TypeScript compiles with no errors
- Component exported as default
- Renders empty state when missing resume or JD
- Renders partial state when only one is present
- Renders tabbed analysis when both present
- All four tabs display correct data from useMatchComparison hook
- ATS score badge color changes based on value
- Keyword gaps grouped by importance
- Recommendations sorted by priority
  </verify>
  <done>MatchComparisonPanel.tsx exists, compiles, displays empty/partial/analysis states, shows ATS score with breakdown, lists keyword gaps, displays semantic similarity, provides actionable recommendations</done>
</task>

</tasks>

<verification>
1. Both files exist in correct locations
2. TypeScript compilation passes (npm run build)
3. MatchComparisonPanel renders three states: empty, partial, analysis
4. useMatchComparison calculates ATS score using weighted formula
5. Keyword gaps extracted and grouped by importance
6. Semantic similarity computed and displayed as percentage
7. Recommendations generated and sorted by priority
8. All tabs use ScrollArea for overflow
9. ATS score badge color reflects score range (green/yellow/red)
</verification>

<success_criteria>
1. User sees empty state when missing resume or JD
2. User sees partial state message when only one is present
3. User sees ATS score with color-coded badge when both present
4. Score breakdown shows 4 weighted components with progress bars
5. Gaps tab lists missing required and preferred keywords
6. Similarity tab displays semantic match percentage
7. Actions tab shows prioritized recommendations with CTA buttons
8. Hook attempts to use Phase 2 utilities if available
</success_criteria>

<output>
After completion, create `.planning/phases/03-job-optimizer-ui/03-04-SUMMARY.md`
</output>
