---
phase: 03-job-optimizer-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/job-optimizer/components/EmptyStatePrompt.tsx
  - src/features/job-optimizer/components/PanelHeader.tsx
  - src/features/job-optimizer/hooks/usePanelLayout.ts
autonomous: true

must_haves:
  truths:
    - "Empty panel states guide users to next action with clear CTAs"
    - "Panel headers have consistent styling with icons and titles"
    - "User's panel layout preferences persist across sessions"
  artifacts:
    - path: "src/features/job-optimizer/components/EmptyStatePrompt.tsx"
      provides: "Reusable empty state component with icon, title, description, CTA"
      min_lines: 40
    - path: "src/features/job-optimizer/components/PanelHeader.tsx"
      provides: "Consistent panel header component"
      min_lines: 25
    - path: "src/features/job-optimizer/hooks/usePanelLayout.ts"
      provides: "Layout persistence hook"
      exports: ["usePanelLayout"]
  key_links:
    - from: "usePanelLayout hook"
      to: "localStorage"
      via: "getItem/setItem for 'job-optimizer-layout'"
      pattern: "localStorage\\.(get|set)Item.*job-optimizer-layout"
---

<objective>
Create foundational UI components for the 3-panel Job Optimizer redesign—empty states, panel headers, and layout persistence.

Purpose: Establish reusable building blocks that all three panels will use, following Swiss-inspired design system consistency.
Output: Three new foundational components/hooks that enable progressive disclosure and user preference persistence.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-job-optimizer-ui/03-RESEARCH.md
@src/features/resume-builder/components/layout/WizardLayout.tsx
@src/shared/ui/card.tsx
@src/shared/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Create EmptyStatePrompt component</name>
  <files>src/features/job-optimizer/components/EmptyStatePrompt.tsx</files>
  <action>
Create reusable empty state component following research pattern from 03-RESEARCH.md Pattern 3.

Component requirements:
- Accept props: { icon: LucideIcon, title: string, description: string, action?: { icon?: LucideIcon, label: string, onClick: () => void } }
- Use Framer Motion for fade-in animation (initial opacity 0, y: 10; animate to opacity 1, y: 0)
- Follow Swiss design: circular icon container (w-20 h-20, bg-muted, rounded-full), centered layout
- Use existing design tokens: text-muted-foreground, font-semibold, max-w-md for description
- Render action button conditionally (only if action prop provided)
- Export as default

Design principles applied:
- Explain WHY empty (via description prop)
- Show HOW to fill it (via action CTA)
- Match existing app visual hierarchy
  </action>
  <verify>
Check component structure:
- TypeScript compiles with no errors
- Component exported as default
- Uses Motion from framer-motion
- Accepts all required props with proper types
  </verify>
  <done>EmptyStatePrompt.tsx exists, compiles, follows Swiss design tokens, animates on mount, renders CTA conditionally</done>
</task>

<task type="auto">
  <name>Create PanelHeader component</name>
  <files>src/features/job-optimizer/components/PanelHeader.tsx</files>
  <action>
Create consistent panel header component for all three panels.

Component requirements:
- Accept props: { icon: LucideIcon, title: string, badge?: string, actions?: React.ReactNode }
- Render header with flex layout (justify-between, items-center)
- Left side: icon + title + optional badge (using Badge component from @/shared/ui/badge)
- Right side: optional action buttons slot (render children if provided)
- Use consistent spacing: p-4 border-b
- Icon styling: w-5 h-5 text-muted-foreground mr-2
- Title styling: font-semibold text-lg
- Badge styling: variant="secondary" ml-2 (only if badge prop provided)
- Export as default

Follow existing pattern from WizardLayout header structure.
  </action>
  <verify>
Check component structure:
- TypeScript compiles with no errors
- Component exported as default
- Renders icon, title, optional badge, optional actions slot
- Uses existing Badge and design tokens
  </verify>
  <done>PanelHeader.tsx exists, compiles, accepts all props, renders consistently across different icon/title/badge combinations</done>
</task>

<task type="auto">
  <name>Create usePanelLayout hook for layout persistence</name>
  <files>src/features/job-optimizer/hooks/usePanelLayout.ts</files>
  <action>
Create custom hook for persisting panel layout sizes to localStorage, following pattern from 03-RESEARCH.md Pattern 1 persistence example.

Hook requirements:
- Storage key: 'job-optimizer-layout'
- Default layout: { horizontal: [35, 65], vertical: [50, 50] } (Resume 35%, Analysis 65% split; JD Analyzer 50%, Match Comparison 50% split)
- Export: usePanelLayout() => { layout, updateHorizontal: (sizes: number[]) => void, updateVertical: (sizes: number[]) => void }
- Load from localStorage on mount using useEffect
- Save to localStorage on update (no debouncing needed—panel resize is low-frequency)
- Type: PanelLayout = { horizontal: [number, number], vertical: [number, number] }

Implementation:
- useState for layout state
- useEffect for initial load (try-catch for JSON parse errors)
- Update functions that save to localStorage and update state
- Export named: export function usePanelLayout()
  </action>
  <verify>
Check hook implementation:
- TypeScript compiles with no errors
- Hook exported as named export
- Returns layout object and update functions
- localStorage operations wrapped in try-catch
- Default values set if localStorage empty
  </verify>
  <done>usePanelLayout.ts exists, compiles, loads from localStorage on mount, persists updates, provides default layout values</done>
</task>

</tasks>

<verification>
1. All three files exist in correct locations
2. TypeScript compilation passes (npm run build)
3. Components follow Swiss design system (use existing tokens)
4. EmptyStatePrompt animates on mount
5. PanelHeader has consistent spacing matching existing panels
6. usePanelLayout persists to localStorage key 'job-optimizer-layout'
</verification>

<success_criteria>
1. EmptyStatePrompt component renders with icon, title, description, and optional CTA
2. PanelHeader component renders with icon, title, optional badge, and action slot
3. usePanelLayout hook loads/saves panel sizes to localStorage
4. All components use existing design tokens and UI primitives
5. No new dependencies added—uses existing framer-motion, lucide-react, shadcn/ui
</success_criteria>

<output>
After completion, create `.planning/phases/03-job-optimizer-ui/03-01-SUMMARY.md`
</output>
